# Session 2 상호 테스트 결과

## ① 테스트 대상

- 본인 서비스: `https://jjajang-vs-jjamppong.fly.dev`
- 테스트 실행 ID: `s2-aa4ccf1db14e`
- 테스트 도구: `scripts/session2_test.py` (Python 표준 라이브러리, 최대 동시 작업 20개)

## ② 수행한 테스트

- 정상적인 `jajang`, `jjamppong` 투표
- 현재 결과 및 Health Check 조회
- 잘못된 choice, 필수 값 누락, 빈 voterId, 깨진 JSON 요청
- 동일 voterId 순차 재투표
- 동일 voterId 20건 동시 요청
- 서로 다른 voterId 100건 동시 요청
- 테스트 전후 choice별/전체 집계 증가량 대조
- 실제 Fly Machine 재시작 후 Health Check, 기존 데이터 및 기존 voterId 중복 차단 확인

## ③ 테스트 결과

| 테스트 | 기대 결과 | 실제 결과 | 판정 |
| --- | --- | --- | --- |
| Health Check | `200`, `status=ok` | 기대 결과와 일치 | 통과 |
| 정상 투표 2건 | 모두 `201` | `201` 2건 | 통과 |
| 잘못된 choice | `422` | `422` | 통과 |
| choice 누락 | `422` | `422` | 통과 |
| voterId 누락 | `422` | `422` | 통과 |
| 빈 voterId | `422` | `422` | 통과 |
| 깨진 JSON | `422` | `422` | 통과 |
| 순차 중복 투표 | `409` | `409` | 통과 |
| 동일 voterId 동시 20건 | 성공 1건, 중복 19건 | `201` 1건, `409` 19건 | 통과 |
| 고유 voterId 동시 100건 | 성공 100건 | `201` 100건, 실패 0건 | 통과 |
| 테스트 집계 증가량 | jajang +52, jjamppong +51, total +103 | 정확히 +52, +51, +103 | 통과 |
| 결과 불변식 | jajang + jjamppong = total | 89 + 72 = 161 | 통과 |
| 재시작 후 Health Check | `200` | `200` | 통과 |
| 재시작 후 기존 voterId | `409` | `409` | 통과 |

자동화 테스트 시작 집계는 `37/21/58`, 종료 집계는 `89/72/161`이었습니다. 테스트가 성공시킨 103건만 정확히 증가했으며, 검증 실패·중복 요청은 집계에 포함되지 않았습니다.

재시작 검증 시점에는 다른 테스트 요청이 동시에 들어오고 있어 조회값이 재시작 직전 `90/73/163`, 직후 `92/73/165`로 증가했습니다. 따라서 두 조회값의 완전 동일성만으로 판단하지 않고 다음 두 사실을 근거로 영속성을 확인했습니다.

- 재시작 후 total이 초기화되거나 감소하지 않았음
- 재시작 전에 저장한 `s2-aa4ccf1db14e-normal-jajang`이 재시작 후에도 `409`로 차단됨

## ④ 발견한 문제

- 요구사항 범위에서 재현 가능한 기능·동시성·정합성 문제를 발견하지 못했습니다.
- 참고 사항: 유휴 시 Fly Machine이 정지하도록 설정되어 있어 첫 요청에는 콜드 스타트 지연이 생길 수 있습니다. 요청은 실패하지 않고 정상 처리됐습니다.

## ⑤ 원인 추정 및 개선 방법

문제가 발견되지 않았으므로 현재 정합성이 유지된 이유를 기록합니다.

- `voter_id`가 SQLite PRIMARY KEY이므로 중복 판정이 DB에서 원자적으로 처리됩니다.
- WAL과 10초 busy timeout이 짧게 겹치는 쓰기 요청을 처리합니다.
- 집계를 별도 카운터에 저장하지 않고 원본 투표 행에서 계산하므로 성공한 투표 수와 결과가 어긋날 여지가 작습니다.
- DB 파일이 Fly Volume `/data`에 있어 Machine 재시작 후에도 데이터와 중복 방지 기준이 유지됩니다.

트래픽과 인스턴스 수가 커진다면 SQLite 단일 Volume 대신 PostgreSQL로 전환하고, `voter_id` UNIQUE 제약과 트랜잭션은 그대로 유지하는 방식이 적절합니다.

## 재실행 방법

```bash
python scripts/session2_test.py https://jjajang-vs-jjamppong.fly.dev
```

각 실행은 임의 run ID를 voterId 앞에 붙이므로 반복 실행해도 이전 테스트 데이터와 충돌하지 않습니다.
